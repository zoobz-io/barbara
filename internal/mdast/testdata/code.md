Fenced code with a language:

```go
func main() {
	println("hello")
}
```

Fenced code with a language and meta:

```js title="example.js"
console.log("hello");
```

Fenced code with no language:

```
plain fenced code
```

Indented code:

    indented code line one
    indented code line two
