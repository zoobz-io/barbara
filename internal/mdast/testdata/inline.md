Text with *emphasis* and _also emphasis_.

Text with **strong** and __also strong__.

Text with ~~strikethrough~~.

Text with `inline code` inside it.

Nested marks: **strong with *emphasis* inside** and *emphasis with `code` inside*.
