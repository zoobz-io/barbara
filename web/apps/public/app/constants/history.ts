/** Releases fetched per page of history; older pages append on demand. */
export const RELEASE_PAGE_SIZE = 10;

/**
 * Pages shown per release card before the rest fold behind "Show all". The
 * live release always shows everything.
 */
export const RELEASE_ENTRY_CAP = 5;
