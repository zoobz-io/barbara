/** The user-menu item that switches colour mode, named for its target. */
export const MODE_LABEL = { light: "Light mode", dark: "Dark mode" } as const;

/** The user-menu item that ends the session. */
export const SIGN_OUT_LABEL = "Sign out";
